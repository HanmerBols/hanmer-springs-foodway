import { Metadata } from "next";
import { WEEKLY_SALE_PAGE_TITLE } from "./content";
import WeeklySalePage from "./page";

export const metadata: Metadata = {
  title: WEEKLY_SALE_PAGE_TITLE,
};

export default WeeklySalePage;
