import { Metadata } from "next";
import QuickBitesPage from "./page";
import { QUICK_BITES_PAGE_TITLE } from "./content";

export const metadata: Metadata = {
  title: QUICK_BITES_PAGE_TITLE,
};

export default QuickBitesPage;
